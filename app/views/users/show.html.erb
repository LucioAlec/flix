<section class="user">
	<%= profile_image(@user) %>
	<h1><%=@user.name %></h1>
	<h2><%= mail_to(@user.email) %> </h2>
	<h4>Member since </h3>
	<p>
	<%= @user.created_at.strftime("%B %Y") %>
	</p>
</section>